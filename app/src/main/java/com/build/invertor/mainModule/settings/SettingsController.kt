package com.build.invertor.mainModule.settings

import android.content.Context
import android.net.Uri
import androidx.activity.result.contract.ActivityResultContracts
import com.build.invertor.mainModule.AbstractController
import com.build.invertor.model.database.Repository
import com.build.invertor.model.modelOld.json.json.JsonFileOpener
import javax.inject.Inject

class SettingsController @Inject constructor(private val context : Context, private val repository: Repository) : AbstractController(context) {




}