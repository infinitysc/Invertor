package com.build.Invertor.model.Parce

import android.os.Parcel
import android.os.Parcelable
import com.build.Invertor.model.csv.User
import com.build.Invertor.model.json.CardInventory

/*class ParceData(
    private val data : Map<String, User>,
    private val json : Map<Pair<String,String>,CardInventory>,

) : Parcelable{
    override fun describeContents(): Int {
        return 0
    }

    override fun writeToParcel(dest: Parcel, flags: Int) {
        dest.writeMap(this.data)
        dest.writeMap(this.json)
    }
    companion object CREATOR : Parcelable.Creator<ParceData>{
        override fun createFromParcel(source: Parcel?): ParceData {
            v


        }

        override fun newArray(size: Int): Array<ParceData> {
            TODO("Not yet implemented")
        }
    }



}*/

